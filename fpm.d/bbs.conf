[global]
error_log = syslog

[bbs]
listen = /run/php-fpm/$pool.socket
pm = dynamic
pm.max_children = 40
pm.start_servers = 10
pm.min_spare_servers = 5
pm.max_spare_servers = 10
php_flag[zlib.output_compression] = on
php_admin_value[open_basedir] = /srv/http/vhosts/$pool.archlinux.de/:/srv/http/vhosts/forum.archlinux.de/:/tmp/:/usr/share/php-xcache/admin/
php_admin_value[disable_functions] = exec,passthru,proc_open,shell_exec,system,popen
; FIXME: Needed for redirects on forum.archlinux.de
php_admin_value[arg_separator.input] = ";&"
